"""Agent dispatch adapters: executor protocol, deterministic mock, manual,
and the provider-neutral bounded subprocess executor (local stubs only in
Phase A; real provider adapters remain unavailable until their own gated
milestone)."""
